# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This repository contains the source code for an "E-commerce After-sales Customer Service and User Review Analysis System" (电商售后客服与用户评价分析系统). The system appears to be structured with three main components:

- `customerManager`: Likely handles customer management functionality
- `customerServer`: Likely contains backend/server logic
- `terminalUser`: Likely handles user-facing terminal or interface components
- `frontend`: For web/user interface components
- `asset`: For static assets (images, styles, etc.)
- `docx`: Contains project documentation

## Development Setup

As this is a newly initialized project, setup instructions will depend on the specific technologies chosen. Common development commands will need to be established as the project progresses.

## Common Development Tasks

When contributing to this codebase, consider these common tasks:

1. **Component Development**: Implement features in the appropriate directories:
   
   - Backend logic in `customerServer/`
   - Customer management features in `customerManager/`
   - User interface/terminal components in `terminalUser/` or `frontend/`
   - Assets in `asset/`

2. **Documentation**: Refer to the documents in `docx/` for detailed requirements and specifications

## Code Architecture

The current structure suggests a modular approach:

- Separation of concerns between customer management, server logic, and user interfaces
- Room for frontend/web implementation
- Asset management for static resources

As the project develops, specific architectural patterns (like MVC, microservices, etc.) will emerge based on implementation choices.

## Getting Started

Since the directories are currently empty, initial development will involve:

1. Choosing appropriate technologies/frameworks
2. Setting up build systems and development environments
3. Implementing core functionality based on the requirements in the documentation

## Note

This CLAUDE.md should be updated as the project evolves to reflect:

- Specific build/test/run commands
- Technology stack decisions
- Architectural patterns adopted
- Development workflows established